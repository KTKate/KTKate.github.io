{
  "name": "portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/pdf.mjs && astro build",
    "preview": "astro preview",
    "sync": "astro sync"
  },
  "dependencies": {
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
