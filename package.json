{
  "name": "portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/pdf.mjs && astro build",
    "preview": "astro preview",
    "sync": "astro sync",
    "test": "node scripts/qa.mjs",
    "test:webkit": "node scripts/webkit-qa.mjs"
  },
  "dependencies": {
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "playwright": "^1.50.0"
  }
}
